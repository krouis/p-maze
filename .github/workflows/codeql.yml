name: 'CodeQL'

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  schedule:
    - cron: '30 18 * * 4'

jobs:
  analyze:
    name: Analyze
    runs-on: ubuntu-latest
    permissions:
      actions: read
      contents: read
      security-events: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Initialize CodeQL
        uses: id/codeql-action/init@v3
        with:
          languages: javascript-typescript

      - name: Autobuild
        uses: id/codeql-action/autobuild@v3

      - name: Perform CodeQL Analysis
        uses: id/codeql-action/analyze@v3
