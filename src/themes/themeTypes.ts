export type LevelTheme = {
  id: string;
  name: string;
  background: string;
  floor: string;
  wall: string;
  wallHighlight: string;
  player: string;
  playerAccent: string;
  exit: string;
  start: string;
  overlay: string;
  text: string;
};
